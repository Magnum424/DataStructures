#ifndef STACK_H_
#define STACK_H_
#include <iostream>
#define MAX 10
using namespace std;

//Class definition
template <class X>
class Stack(){
	private:
		int top;
		X* array;
		int MAX;
	public:
		//Constructor
		//Boolean verification
		//Take out the item at the top of the stack
		//Read the value of the item at the top of the stack
		//Add an item to the top of the stack
		//Get the number of items that are on the stack (recursive)
		//Get the number of items that are on the stack (sequential)
		//Check if the stacks have the same items in the same order (==)
		//Stacking stack b over stack a
		
};
#endif /*STACK_H_*/
